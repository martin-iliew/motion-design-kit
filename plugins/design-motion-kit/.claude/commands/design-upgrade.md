---
description: Audit and automatically modernize landing-page HTML, React, Vue, Svelte, or related frontend files using the merged design-system pipeline. Optional leading mode syntax: mode: fast|balanced|premium. Default: balanced.
argument-hint: "mode: premium path/to/file.html" or "path/to/home.html path/to/home.css path/to/Hero.tsx"
allowed-tools: Read, Edit, WebSearch, WebFetch, Agent, Bash
---

Use the `Skill` tool to invoke the `design-upgrade` skill, passing "$ARGUMENTS" as the file(s) to upgrade. If no leading `mode:` prefix is present, the skill defaults to `balanced`.

