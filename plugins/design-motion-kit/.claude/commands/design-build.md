---
description: Build or modernize landing-page design with the merged design-system pipeline. Optional leading mode syntax: mode: fast|balanced|premium. Default: balanced.
argument-hint: "mode: fast src/index.html modernize the hero using the brief bundle" or "src/components/Hero.tsx add enterprise proof and stronger hierarchy"
allowed-tools: Read, Edit, WebFetch, WebSearch, Bash
---

Use the `Skill` tool to invoke the `design-build` skill, passing "$ARGUMENTS" as the design request. If no leading `mode:` prefix is present, the skill defaults to `balanced`.

