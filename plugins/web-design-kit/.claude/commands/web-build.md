---
description: Write or modernize landing-page design in existing HTML, React, Vue, or Svelte files, or generate conversion-first layout and section patterns for homepage work. Optional leading mode syntax: mode: fast|balanced|premium. Default: balanced.
argument-hint: "mode: fast src/index.html modernize the hero" or "src/components/Hero.tsx add enterprise proof and tighter layout"
allowed-tools: Read, Edit, WebFetch, WebSearch, Bash
---

Use the `Skill` tool to invoke the `web-build` skill, passing "$ARGUMENTS" as the design request. If no leading `mode:` prefix is present, the skill defaults to `balanced`.
