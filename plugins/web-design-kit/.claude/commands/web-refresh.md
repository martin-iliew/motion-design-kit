---
description: Research current landing-page design baselines and rerank the design library catalog and lookup index.
argument-hint: "refresh the library"
allowed-tools: Read, Edit, WebSearch, WebFetch, Bash
---

Use the `Skill` tool to invoke the `web-refresh` skill.
