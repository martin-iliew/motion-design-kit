---
description: Research current landing-page design patterns used by major product companies and generate new entries in the shared design library.
argument-hint: "hero patterns for AI tools" or leave empty to run a general pass
allowed-tools: Read, Edit, WebSearch, WebFetch, Agent, Bash
---

Use the `Skill` tool to invoke the `web-discover` skill.
