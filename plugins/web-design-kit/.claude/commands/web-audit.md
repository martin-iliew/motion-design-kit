---
description: Review landing-page markup, styles, and section structure for hierarchy, proof, CTA clarity, conversion friction, and visual consistency without editing files.
argument-hint: "src/pages/home.html src/styles/home.css"
allowed-tools: Read, WebSearch, WebFetch, Bash
---

Use the `Skill` tool to invoke the `web-audit` skill, passing "$ARGUMENTS" as the file or files to audit.
