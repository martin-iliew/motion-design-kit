---
description: Write or modernize motion in existing HTML, React, Vue, or Svelte files, or generate animation code for GSAP, CSS, and component frameworks
argument-hint: "src/index.html add scroll reveal" or "src/components/Hero.tsx add a premium intro timeline"
allowed-tools: Read, Edit, WebFetch, WebSearch, Bash
---

Use the `Skill` tool to invoke the `motion-dev` skill, passing "$ARGUMENTS" as the animation request.
