---
description: Write, fix, translate, or generate animation code for GSAP, CSS, Tailwind, React, or Vue
argument-hint: "animate the hero card on scroll" or "convert this GSAP to React"
allowed-tools: Read, Edit, WebFetch, WebSearch
---

Use the `Skill` tool to invoke the `motion-dev` skill, passing "$ARGUMENTS" as the animation request.
