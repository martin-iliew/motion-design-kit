---
description: Audit an HTML/JS/CSS file for animation quality, performance, conflicts, and 2026-readiness
argument-hint: path/to/file.html
allowed-tools: Read, Edit, WebSearch
---

Use the `Skill` tool to invoke the `motion-audit` skill, passing "$ARGUMENTS" as the file to audit.
