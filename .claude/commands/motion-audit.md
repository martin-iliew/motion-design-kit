---
description: Audit HTML, JS, TS, React, Vue, or Svelte files for animation quality, performance, conflicts, and 2026-readiness
argument-hint: path/to/file.html [path/to/component.tsx ...]
allowed-tools: Read, Edit, WebSearch, Bash
---

Use the `Skill` tool to invoke the `motion-audit` skill, passing "$ARGUMENTS" as the file to audit.
