---
description: Write or modernize motion in existing HTML, React, Vue, or Svelte files, or generate animation code for GSAP, CSS, and component frameworks. Optional leading mode syntax: mode: fast|balanced|premium. Default: balanced.
argument-hint: "mode: fast src/index.html add scroll reveal" or "src/components/Hero.tsx add a premium intro timeline"
allowed-tools: Read, Edit, WebFetch, WebSearch, Bash
---

Use the `Skill` tool to invoke the `motion-build` skill, passing "$ARGUMENTS" as the animation request. If no leading `mode:` prefix is present, the skill defaults to `balanced`.
